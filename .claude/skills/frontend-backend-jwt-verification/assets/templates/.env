# JWT Configuration (must match BETTER_AUTH_SECRET in frontend/.env.local)
JWT_SECRET_KEY=your-super-secret-key-here-min-32-chars

# Must match Better Auth JWT configuration
JWT_ALGORITHM=HS256
JWT_ISSUER=https://your-domain.com
JWT_AUDIENCE=https://api.your-domain.com

# Database URL
DATABASE_URL=postgresql://user:pass@localhost:5432/app

# CORS origins (comma-separated for multiple)
BACKEND_CORS_ORIGINS=http://localhost:3000
